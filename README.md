like the normal one, except:
- sponsor images display for 10 seconds
- sponsor images have a fade transition
- **jams are able to be started from a timeout without first ending the timeout** (post-timeout lineup can still be activated by ending the timeout)